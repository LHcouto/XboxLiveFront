import CreateProfile from "components/CreateProfile";
import * as S from "./style";

const Create = () => {
  return (
    <S.Create>
      <S.CreateContent>
        <CreateProfile />
      </S.CreateContent>
    </S.Create>
  );
};

export default Create;
